# Database Migrations

Placeholder directory for future schema migrations (Phase 1+).

## Phase 0

- PostgreSQL runs **locally via Docker Compose only**
- Initial schema bootstrap: `database/init/01-init.sql`
- No business tables yet — only `retail.schema_version` for connectivity checks

## Future Phases

Migration files will be added here when the retail data model is implemented in Phase 1.
